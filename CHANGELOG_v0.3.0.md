# 蜗牛群聊精灵 v0.3.0 商业化基础版

## 本版重点

本版把原有单客户微信群机器人后台升级为可继续扩展的平台化商业架构，保留现有 AI、微信机器人、群管理、用户、商城、恋爱、婚姻、宝宝与系统设置功能。

### 代理商与购买客户

- 新增代理商 `agents`，支持佣金、预付/买断、混合三种代理模式。
- 新增购买客户 `customers`，购买程序的微信号作为客户 Owner。
- 新增客户与微信群所有权 `customer_group_ownerships`。
- 微信号可管理多个群，实际上限取客户套餐上限与全局微信风控上限的较小值。
- `wechat_max_groups_per_account` 可在系统参数中随微信风控调整。

### 统一授权/兑换码引擎

- 群管理授权码 `group_license`。
- 钻石兑换码 `diamonds`。
- 预留会员码、伴侣扩容码、宝宝扩容码。
- 完整代码只在生成时返回；数据库保存 SHA-256 哈希，不保存可直接盗用的明文码。
- 记录代理商、购买客户、微信号、微信群、兑换类型、数值和精确兑换时间。
- 群管理码兑换后绑定“购买者微信号 + 当前微信群”。
- 钻石码兑换后进入付费钻石余额。

### 用户资产

- 保留蜗币 `coins`。
- 新增 `paid_diamonds`：真实购买/代理商钻石码到账。
- 新增 `bonus_diamonds`：会员赠送、活动、群管理员赠送。
- 新增 `diamond_transactions` 完整钻石流水。
- 消费钻石时优先使用奖励钻石，再使用付费钻石。
- 群管理员只能赠送蜗币或奖励钻石，不能伪造付费钻石。

### 会员商业化基础

- 新增会员方案 `membership_plans`。
- 新增会员商品 `membership_products`：价格、钻石价、天数、赠钻数量与赠钻模式独立配置。
- 新增真实用户会员记录 `user_memberships`。
- 继续兼容现有 `users.member_level / member_expire_at`。
- 支持使用钻石购买会员商品。
- 会员赠钻进入奖励钻石。

### 永久扩容权益

- 新增 `user_entitlements`。
- 钻石可购买永久 `extra_partner_slots` 和 `extra_baby_slots`。
- 伴侣与宝宝限制计算改为：会员基础额度 + 用户永久扩容额度。
- 会员过期不删除既有关系/宝宝，只影响后续新增能力。

### 群级独立权限基础

- 新增 `group_members`，同一个微信号可在不同群拥有不同身份。
- 新增 `group_admin_permissions`，为后续群内授权命令和逐项权限做准备。
- 新增 `admin_asset_grants`，记录谁、在哪个客户/群、给哪个用户赠送了多少资产。
- 新增 `group_admin_sessions`，为后续“@机器人 管理后台”短时网页登录做数据基础。

### 后台

新增“代理授权”模块：

- 代理商列表与创建。
- 购买客户列表。
- 授权码/钻石码生成。
- 授权码状态查询。
- 兑换审计记录。
- 会员方案/会员商品查看。
- 用户中心显示付费钻石、奖励钻石及总钻石。

## 下一阶段已经预留的数据结构

- 代理佣金 `agent_commissions`。
- 代理授权库存流水 `agent_license_transactions`。
- 群管理员短时网页 Session。
- 会员兑换码、伴侣扩容码、宝宝扩容码。
- 群欢迎语、群重要聊天记录、图片/视频收藏与抽奖模块可在本架构上继续开发。
